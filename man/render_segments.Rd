% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lines.R
\name{render_segments}
\alias{render_segments}
\title{Render line segments directly to an image}
\usage{
render_segments(
  from,
  to,
  colors = NULL,
  width = 1,
  camera = NULL,
  options = render_options(),
  lit = FALSE
)
}
\arguments{
\item{from}{Nx3 numeric matrix of segment start points (or a length-3 vector).}

\item{to}{Nx3 numeric matrix of segment end points (same number of rows as
\code{from}).}

\item{colors}{RGBA colour(s): a single vector applied to all segments, or an
Nx4 numeric matrix.  \code{NULL} (the default) uses the \code{default_color}
of the render options.}

\item{width}{Line width in pixels (default 1).}

\item{camera}{A camera list, e.g. from \code{\link{camera}} or
\code{\link{camera_auto}}.  Defaults to a camera framing the segments.}

\item{options}{Render options, see \code{\link{render_options}}.}

\item{lit}{Whether to apply lighting (default \code{FALSE}, flat colour).}
}
\value{
An image list, see \code{\link{render_scene}}.
}
\description{
Convenience wrapper around \code{\link{line_layer}} for the case where no
meshes are involved: the segments are drawn (with a screen-space width) into
an image and nothing else.  To combine lines with meshes, add the layer to a
scene instead and render that scene.
}
\examples{
from <- matrix(c(-1, 0, 0, 0, -1, 0), ncol = 3, byrow = TRUE)
to   <- matrix(c(1, 0, 0, 0, 1, 0), ncol = 3, byrow = TRUE)
img <- render_segments(from, to, colors = c(1, 0, 0, 1), width = 3)
tmp_file <- tempfile(fileext = ".png")
write_png(img, tmp_file)

}
\seealso{
\code{\link{line_layer}}, \code{\link{render_points}}
}
