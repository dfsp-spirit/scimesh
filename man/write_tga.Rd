% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image_export.R
\name{write_tga}
\alias{write_tga}
\title{Write a rendered image to a TGA file}
\usage{
write_tga(image, filename, use24bit = FALSE)
}
\arguments{
\item{image}{An image list returned by \code{render_mesh()} or
\code{render_scene()}.}

\item{filename}{Output TGA file path.}

\item{use24bit}{If \code{TRUE}, write 24-bit RGB (no alpha channel).
The default \code{FALSE} writes 32-bit RGBA.}
}
\value{
No return value; called for side effects.
}
\description{
Writes the output of \code{render_mesh()} or \code{render_scene()}
to a TGA file using scimesh's own C++ TGA writer (no external
dependencies).  TGA output is uncompressed true-color.
}
\examples{
mesh <- generate_cuboid(c(0, 0, 0), c(1, 1, 1))
img <- render_mesh(mesh$vertices, mesh$triangles)
tmp_file <- tempfile(fileext = ".tga")
write_tga(img, tmp_file)

}
