% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image_export.R
\name{image_apply_gamma}
\alias{image_apply_gamma}
\title{Apply gamma correction to an image}
\usage{
image_apply_gamma(image, gamma = 2.2)
}
\arguments{
\item{image}{An image list returned by \code{render_mesh()} or
\code{render_scene()}.}

\item{gamma}{Gamma value.  Default 2.2 for sRGB-like contrast.}
}
\value{
A new image list with gamma-corrected pixel data.
}
\description{
Applies gamma encoding to the RGB channels of a rendered image.
Formula: \code{value^(1/gamma)}.  Default 1.0 = no change.
Values > 1.0 (e.g. 2.2) brighten midtones, matching the sRGB
transfer function used by GPU renderers.
}
\examples{
\dontrun{
img <- render_mesh(cube$vertices, cube$triangles)
img <- image_apply_gamma(img, gamma = 2.2)
}

}
