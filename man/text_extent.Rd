% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{text_extent}
\alias{text_extent}
\title{Measure text}
\usage{
text_extent(text, size = 18, font_file = NULL, line_spacing = 1.2)
}
\arguments{
\item{text}{Character vector of labels (UTF-8; \code{"\n"} for line breaks).}

\item{size}{Text height in output pixels (default 18).}

\item{font_file}{Path to a \code{.ttf} file, or \code{NULL} for the bundled
font (see \code{\link{default_font}()}).}

\item{line_spacing}{Distance between lines, as a multiple of the glyph box
height (default 1.2).}
}
\value{
A data frame with one row per input string and the columns
  \code{text}, \code{width} (widest line), \code{height} (whole block),
  \code{ascent}, \code{descent} and \code{lines}.
}
\description{
Computes the size of the text box that \code{\link{text_layer}} uses, which
is needed to place labels relative to each other (for example to right-align
a caption, or to keep two labels from overlapping).  Multi-line labels are
measured as a whole, using the same line spacing as the renderer.
}
\examples{
text_extent("anterior", size = 20)
text_extent(c("left hemisphere", "right hemisphere"), size = 16)

}
\seealso{
\code{\link{text_layer}}
}
