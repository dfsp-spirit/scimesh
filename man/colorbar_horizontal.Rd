% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colorbar.R
\name{colorbar_horizontal}
\alias{colorbar_horizontal}
\title{Generate a horizontal colorbar image}
\usage{
colorbar_horizontal(
  colormap,
  n_colors = 256L,
  width = 600L,
  height = 80L,
  ticks = NULL,
  tick_labels = NULL,
  data_range = c(0, 1),
  label_cex = 1,
  title = NULL,
  background = c(1, 1, 1, 1)
)
}
\arguments{
\item{colormap}{A vector of colors or a function returning colors
(e.g. \code{grDevices::hcl.colors}).}

\item{n_colors}{Number of discrete color segments in the gradient.}

\item{width}{Output width in pixels.}

\item{height}{Output height in pixels.}

\item{ticks}{Numeric vector of tick positions in data units
(matching \code{data_range}). If \code{NULL}, ticks are computed
automatically via \code{pretty()} restricted to the data range.}

\item{tick_labels}{Character vector of tick labels. If \code{NULL},
defaults to formatted tick values.}

\item{data_range}{The data range that \code{ticks} are specified in.
Defaults to \code{c(0, 1)}.}

\item{label_cex}{Label size multiplier.}

\item{title}{Optional title string drawn above the color strip
(horizontal) or to the right (vertical).}

\item{background}{Background RGBA color (0-1 scale).}
}
\value{
A 3D array of dimensions (height, width, 4) with values in
  \code{[0, 1]}.
}
\description{
Creates a horizontal colorbar as a 4-channel RGBA array. The color
strip and optional tick labels are rendered to PNG using base R
graphics (headless-safe). Returns a 3D array suitable for
\code{image_to_array()} or direct composition.
}
\examples{
cbar <- colorbar_horizontal(viridis_colormap, data_range = c(-2, 3),
    title = "Value")
dim(cbar)  # height x width x 4

}
