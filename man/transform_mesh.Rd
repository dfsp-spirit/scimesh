% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh_utils.R
\name{transform_mesh}
\alias{transform_mesh}
\title{Apply a 4x4 transformation matrix to a mesh}
\usage{
transform_mesh(mesh, matrix)
}
\arguments{
\item{mesh}{A mesh descriptor list with \code{vertices} and
\code{triangles}, as returned by \code{render_mesh()} or built
by \code{scimesh_generate_multi_spheres()} etc.}

\item{matrix}{A 4x4 numeric matrix.}
}
\value{
A new mesh descriptor list with transformed vertices.
}
\description{
Transforms all vertex positions in a mesh by a 4x4 homogeneous
matrix (applied as \code{M * (x, y, z, 1)^T}).  Vertex colors and
normals are untouched.
}
\examples{
mesh <- generate_cuboid(c(0, 0, 0), c(1, 1, 1))
mat <- diag(4)
mat[1:3, 4] <- c(2, 3, 4)
translated <- transform_mesh(mesh, mat)
translated$vertices[1, ]

}
