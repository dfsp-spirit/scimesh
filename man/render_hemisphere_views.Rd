% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/views.R
\name{render_hemisphere_views}
\alias{render_hemisphere_views}
\title{Render standard neuroimaging figure layout}
\usage{
render_hemisphere_views(
  mesh,
  hemisphere = c("left", "right"),
  width = 800L,
  height = 600L,
  options = NULL,
  colorbar = NULL,
  colorbar_height = 80L,
  background = c(1, 1, 1, 1),
  fov = 45,
  margin = 1.1,
  ...
)
}
\arguments{
\item{mesh}{A mesh descriptor list with \code{vertices},
\code{triangles}, and optionally \code{colors}.}

\item{hemisphere}{Which hemisphere: \code{"left"} or \code{"right"}.}

\item{width}{Width of each sub-image.}

\item{height}{Height of each sub-image.}

\item{options}{Additional render options (passed to
\code{render_options()}).}

\item{colorbar}{Optional colorbar array from
\code{colorbar_horizontal()}.}

\item{colorbar_height}{Height of the colorbar in pixels.}

\item{background}{Background RGBA color.}

\item{fov}{Field of view in degrees.}

\item{margin}{Camera margin factor.}

\item{...}{Additional arguments passed to \code{render_options()}.}
}
\value{
A composed image list with lateral and medial views side by
  side, with optional colorbar.
}
\description{
Renders a hemisphere mesh from lateral and medial views and
composes them side by side, with an optional colorbar below.
}
\examples{
\dontrun{
# Render lateral + medial views of a brain mesh
img <- render_hemisphere_views(brain_mesh, hemisphere = "left",
    draw_colorbar = TRUE)
write_png(img, "brain_views.png")
}

}
